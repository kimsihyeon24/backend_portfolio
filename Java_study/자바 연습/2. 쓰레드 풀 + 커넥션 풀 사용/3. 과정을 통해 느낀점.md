느낀점

12.26

쓰레드풀과 커넥션풀을 활용해 효율적인 jdbc와 데이터베이스 통신 과정을 구현했다.

오늘은 전반적인 틀만 구현 + insert 만 구현해냈다.

객체지향적으로 나누어서 코드를 설계했다.

쓰레드풀에 여러가지 종류가 있고, 각 특징들을 파악해서 사용하는 것이 효율적일 듯

내 프로젝트는 가장 많이 쓰는 FIxed사용

커넥션풀 각 벤치마크 점수를 보니 HIKARI가 가장 좋음 이것쓰면 될듯

여기서 HIKARI는 sj@jf인가 이놈에 의존성이 있어서 라이브러리 삽입함

사용할수록 maven,gradle이 얼마나 좋은 것인지 느끼게 된다.

내일은 전체 메서드를 작성하고, 여러가지 테스트를 해보면서 좀 더 가지고 놀아볼 것이다.



12.27

나머지 조회, 삭제, 업데이트를 완료함 특히 조회 단계에서 Future객체를 사용해 처리하는 것이 기억에 남음

또한 커넥션풀 시간을 10초로 설정하고

작업 쓰레드에서 10초를 기다리고 작업하라는 코드를 짠후 5개의 삽입 수행을 하자

Hikari에서 에러(?)로그를 뱉어냈다. 이미지를 보면 알수있고, 내가 코드를 정상적으로 작동시켰구나를 알 수 있었고, 좀더 잘 이해할 수 있게 되었다.

하지만 여기서 중요한 점은 이렇게 에러가 발생할 때 데이터 유실을 원치 않기 때문에 어떻게 처리할까이다.

chat gpt에게 물어보니 3가지 방식이 있다.

1. 기다리는 시간 단순 증가
2. 큐에 쌓아놓고 처리 ex) kafka, RabbitMQ, SQS
3. 조금 기다렸다가 재시도..



쓰레드 풀과 커넥션 풀에 대한 이해가 높아졌다.

물론 실무에서 사용할 정도의 수준은 아니지만 아직 초보이기에 개념을 이해하고 학습했다는 것에 만족하고 다음단계로 넘어가겠다.

